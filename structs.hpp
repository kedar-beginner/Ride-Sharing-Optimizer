#ifndef STRUCTS_HPP
#define STRUCTS_HPP

#include <string>
using namespace std;

struct Rider {
    string name;
    string pickup;
    string drop;
};

struct Driver {
    string name;
    string location;
};

#endif
